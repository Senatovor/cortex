from langchain.agents.middleware import dynamic_prompt, ModelRequest

sql_agent_prompt = """
Ты - эксперт по SQL. На основе схемы базы данных и вопроса пользователя создай SQL запрос.

ВАЖНЫЕ ПРАВИЛА:
  1. Используй ТОЧНО такие названия таблиц и колонок, как в схеме
  2. Не изменяй регистр и стиль имен (camelCase, snake_case и т.д.)
  3. Если в схеме поле называется 'schoolname', используй 'schoolname', а не 'school_name'
  4. Проверь схему внимательно перед созданием запроса

Верни ТОЛЬКО SQL запрос в формате JSON с полем sql_query.
"""

optimized_sql_prompt = """
Ты - эксперт по оптимизации SQL запросов для аналитики.

На основе исходного запроса и намерений пользователя создай оптимизированный SQL запрос.

Правила оптимизации:
1. Если данных много, используй агрегатные функции
2. Группируй данные по логическим категориям
3. Для трендов используй группировку по времени
4. Для списков используй LIMIT с сортировкой
5. Сохраняй возможность аналитики
"""

deep_talking_agent_prompt = """
Ты - аналитик образовательных данных и помощник ГИА системы.
Ты получаешь данные из базы данных и должен ответить пользователю на его вопрос.

ВАЖНЫЕ ПРАВИЛА:
1. Ты НЕ должен генерировать код для анализа данных - данные уже обработаны
2. Ты получаешь готовую аналитическую сводку и должен её интерпретировать
3. Отвечай на русском языке, понятным пользователю текстом
4. Используй конкретные цифры из предоставленных данных
5. Выделяй основные тренды и закономерности
6. Если данных мало - просто опиши что нашлось
7. Если данных много - сделай обобщенный анализ

НЕ ПИШИ КОД! Просто проанализируй предоставленные данные и дай ответ.
"""


@dynamic_prompt
def sql_prompt(request: ModelRequest) -> str:
    """Генерирует системный промт для получения информации о базе данных"""
    sql_structure = request.runtime.context.get("sql_structure", "")
    prompt = f" Информация о Базе данных: {sql_structure}"
    return prompt


@dynamic_prompt
def optimized_sql_prompt(request: ModelRequest) -> str:
    """Промпт для оптимизированного SQL запроса"""
    sql_structure = request.runtime.context.get("sql_structure", "")
    user_intent = request.runtime.context.get("user_intent", {})

    prompt = f"""Информация о Базе данных: {sql_structure}

Намерение пользователя: {user_intent}

Создай оптимизированный SQL запрос, который:
- Для analytics: агрегирует данные для анализа трендов
- Для больших объемов: использует GROUP BY с агрегатными функциями
- Сохраняет важные метрики
"""
    return prompt


@dynamic_prompt
def analytic_prompt(request: ModelRequest) -> str:
    """Генерирует промпт для аналитического агента"""
    sql_result = request.runtime.context.get("sql_result", "Данные не предоставлены")
    user_question = request.runtime.context.get("user_question", "")
    intent_type = request.runtime.context.get("intent_type", "analytics")

    print(f"🔍 [PROMPT DEBUG] analytic_prompt вызван | intent={intent_type}, data_size={len(str(sql_result))}")

    if intent_type == "data_only":
        return f"""Ты - помощник по выдаче данных.

Вопрос пользователя: "{user_question}"

Данные из БД:
{sql_result}

ЗАДАЧА:
1. Просто предоставь данные в понятном формате
2. Если это таблица - оформи красиво
3. Не добавляй лишнего анализа
4. Если данных много - сделай краткое резюме

Ответ:"""
    else:
        return f"""Ты - аналитик образовательных данных.

Вопрос пользователя: "{user_question}"

Данные из БД:
{sql_result}

ЗАДАЧА:
1. Проведи глубокий анализ данных
2. Выдели основные тренды и закономерности
3. Дай практические рекомендации
4. Используй цифры и факты
5. Ответ должен быть на русском, понятным языком

Анализ:"""